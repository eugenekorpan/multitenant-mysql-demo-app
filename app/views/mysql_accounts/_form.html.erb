<%= form_for(@mysql_account) do |f| %>
  <% if @mysql_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mysql_account.errors.count, "error") %> prohibited this mysql_account from being saved:</h2>

      <ul>
      <% @mysql_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
